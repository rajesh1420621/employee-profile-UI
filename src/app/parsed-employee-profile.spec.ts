import { ParsedEmployeeProfile } from './parsed-employee-profile';

describe('ParsedEmployeeProfile', () => {
  it('should create an instance', () => {
    expect(new ParsedEmployeeProfile()).toBeTruthy();
  });
});
